# 统一换行符，避免跨平台协作时整个文件显示为「已修改」
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则可能无法执行
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
