# AI 工具本地配置
**/.claude/settings.local.json
.cursor/
.aider*
.continue/
.cody/

# skill 是插件生成维护的，升级时整批替换，进版本库只会淹掉真正的改动。
# 注意：这条只管未跟踪的文件，已经入库的那几个 skill 不受影响，照常提交。
**/.claude/skills/

# 密钥和凭证
*.pem
*.key
credentials.json
.npmrc
.aws/
.ssh/

# 各类项目的环境变量文件（常含 API Key）。
# 用 .env.* 覆盖 .env.local / .env.production / .env.development 等变体，
# 再用 ! 把 .env.example 放回来 —— 那是空值模板，应该入库。
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# 资料
ziliao/

# 打包产物。放这里是为了和 src-tauri/target 分开——那个目录清理磁盘时容易被整个端掉
actual/gravity-flip-dist/
actual/personal/

# 该目录保留在本地，不再纳入版本管理（已从远程仓库移除）
claude_deepseek/
